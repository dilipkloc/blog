<h1> articles index </h1>
<% @articles.each do |article|%>
 <b> Category: <%= Category.find(article.category_id).name %> </b> <br/>
 <%= article.id %>
 <%= image_tag(article.feature_image_url.url, size: "100x50") if article.feature_image_url %><br/>
  <%= article.title %><br/>
  <%= article.body %><br/>
  <%= article.category_id %><br/>
  <%= article.publish_date %><br/>
  <% if article.is_published %>
    It is published
  <% else %>
    It is not published
  <% end %>
    <%= link_to "Delete", article , method: :delete, data: { confirm: "sure?" } %><br/>
  <br/>

  <%#= #image_tag(article.feature_image_url) %> 
  
   <br/><br/>
  
<% end %>